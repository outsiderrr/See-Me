# Fathom（潜心）—— 状态快照与待办

> 日期：2026-07-26 深夜。**设计的唯一权威仍是 `2026-07-26-see-me-v2-web-pipeline.md`**，
> 本文只记「现在到哪了 / 下一步做什么 / 环境有哪些坑」，会随进度过期。
> 长期记忆：`~/.claude/projects/-Users-outsider-Desktop-see-me/memory/`

## 0. 一句话

产品已更名 **Fathom / 潜心**。后端 + 免登录卡 + A 端控制台**都已部署在东京服务器上跑着**，
写→打标签→建卡→发链接的闭环在浏览器里可以走通。剩下的大头是**把语料灌进去**（P4 导入器）
和**上 HTTPS**（P1，卡在域名没买）。

## 1. 已完成且在线（`main` = 103ffe8）

服务器 `http://8.209.239.47`，栈在 `/opt/see-me`（路径刻意没跟着改名，见 §4）：

| 部分 | 状态 |
|---|---|
| 后端 M0–M4 | OTP 登录、笔记/标签 CRUD、权限引擎（交集+排除）、兑换限流、owner-preview |
| B 端登录阅读 | `/` |
| **P2 免登录卡** | `/c/<14位slug>`，无账号可读，数据层零读者记录。外网实测通过 |
| **P3 控制台** | `/console`：库流 / 收件箱 / 写编辑删搜 / 打标签 / 卡管理（建卡·分享·轮换·推进·预览·删卡） |
| 改名 | See Me → Fathom（中文名「潜心」）已落地并部署 |
| 测试 | 29 个，权限面全覆盖 |

**登录方式（重要）**：`SMS_DRIVER=dev`，验证码**不发短信、只打进容器日志**。所以登录要 shell：

```bash
# 浏览器打开 http://8.209.239.47/console 输手机号点发送，然后：
cd /opt/see-me && docker compose logs --tail=100 app | grep 'sms:dev' | tail -1
```

用户在服务器上的账号手机号：**+8618587335461**（这是他本人的号；`+86` 前缀必须带）。

## 2. 已设计确认、但还没开发

按优先级：

### 2.1 P4 导入器（下一个动工，设计已完全定稿）

**提炼不由代码调模型**——这是本次会话的重大转向（见记忆里的理由）。管线是：

```
原始/  ──[用户把提示词交给自己的 agentic AI，他用 Codex]──▶  库/*.md  ──▶ 导入器 ──▶ 服务器
```

- 数据湖：`~/通用空间/潜心/{原始,库}/`（**不是** spec 里的 `~/SeeMeLake/`，也刻意不在 iCloud 托管的桌面上；结构和硬规则见该目录的 `README.md`）
- 待写：`tools/import/`（纯 `.mjs`，零构建，`node` 直接跑）
  - 解析 `库/*.md` 的标准格式（下面 §2.2）
  - **校验**（关键，不能只信提示词）：方括号残留 / 与原文逐字重合度 / 字段完整 / 标签词表
  - 幂等：`source + 内容指纹` 判重，state 文件放湖里
  - 无标签入库，溯源写进**笔记首行**（不打 `#录音` 标签，理由见 §2.3）
- 待写：`tools/import/PROMPT.md`（正式版提炼提示词）
- 已有：`tools/import/parse.mjs`（解析**原始素材**，只给校验器做逐字重合比对用；管线主解析不需要它）

**首批要灌的东西已经就绪**：`~/通用空间/潜心/库/2026-W29-试跑.md`，Codex 提炼的 **54 条原子笔记**，
来自 13 份素材，校验全绿（正文中位 61 字 / 最长 131，无方括号残留，无链接，非复制粘贴）。

**上传路径（刻意不从本机 POST）**：`scp` 文件到服务器，在服务器上对 `localhost` 入库。
服务器还是明文 HTTP，54 条私人笔记不该裸奔穿过公网。

### 2.2 标准笔记格式（契约，整条管线的地基）

```markdown
---
source: 语音备忘录/2026-W27/raw/01-北京路.md   # 相对「原始/」
kind: voice                                   # voice | chat
dated: 2026-07-06                             # 素材里的原始日期
suggest: [私密, 价值观]                        # 0-2 个，来自用户已有标签
---

## 一句话标题

正文。
```

- `source` 是幂等的锚
- `dated` **只写进笔记首行，绝不回填 `created_at`**（决策 8 红线：回填会让冻结卡的权限被动扩大）
- `suggest` **本轮不上传**（没有存它的列，加列是个迁移）

### 2.3 一处 spec 内部冲突，已决议

交接文档同时说「无标签入库」（收件箱 = 无标签队列）和「溯源用 `#录音`/`#对话` 标签」。
P3 的收件箱是字面的 `noteTags: { none: {} }`——**打上 `#录音` 会让导入的笔记永远进不了收件箱，
直接卡死审校闭环**。决议：溯源信息写进**笔记首行**，不打标签。决策 8 本来就规定
「录音原始日期写进笔记首行」，已经接受这类元信息对读者可见。零迁移。

### 2.4 P1 域名 + Caddy + HTTPS（卡在用户买域名）

- 名字已定：英文 **Fathom**、中文**潜心**，域名方向 **`fathomlog.com`**（`fathom` 裸名在所有主流后缀全被占；`fathomlog` 整个命名空间空着：.com/.net/.io/.co/.app/.dev/.ink 都可注册）
- 买完加 Caddy 服务（80/443 自动证书 + HSTS），app 不再直接暴露 80
- **在此之前不要对外发免登录链接**：slug 就是那张卡的全部凭证，明文 HTTP 会把它暴露给链路上每一跳。控制台里已经为此挂了红字警告
- 顺带一起解决：`lib/clientIp.ts` 目前**无条件信任 XFF**（可伪造）。真正的规则要等知道代理拓扑才能定

### 2.5 P3 剩下的两块

- **标签管理**：置顶 / 改名 / 双模式删除（detach vs delete_notes，后端 `tags.ts` 已实现，缺界面）
- **邀请图片生成**：canvas 纸页风格，卡名 + 链接二维码或 8 位码，下载 PNG 发社交媒体（QR 用 vendored MIT 小库）
- **建议标签预选**：等用户手打完这 54 条、测出真实摩擦再做。要加一列存 `suggest`

### 2.6 P5 收尾

- 阿里云短信签名过审 → `aliyun` 驱动（在此之前登录都要读容器日志）
- `pg_dump` 定时备份
- 数据湖 rsync 备份到服务器（命令已写在湖的 `README.md`，还没设定时）

### 2.7 Backlog（明确不做，别自己捡起来）

iOS 继续开发（已冻结）、原音频上传、涌现分析、export 回流、官方 ChatGPT 导出、
读者侧任何 AI、per-note 反向可见性查询、A 端持卡人可见面、公开卡阅读统计。

## 3. 用户侧待办

1. **买 `fathomlog.com`**（P1 的唯一前置）
2. ~~实验室原件~~ 已删（2026-08-02：删前复验发现用户中间又采集了 **2026-W30**（8 个新对话），
   先同步进湖、27 个文件复验全等后才删。只删了 `记录汇总/` 和 `distilled-试跑.md`，
   `实验室/` 其余内容没碰。**W30 还没提炼**——下次跑提炼提示词时一并处理）
3. ~~空壳 PNG~~ 已删（2026-08-02：39 个，另有 4 个 iCloud 冲突空壳副本「xxx 2.ts」一并清掉，
   正本与 git 记录核对无误）
4. 手打这 54 条笔记的标签，然后告诉我累不累——这个数决定要不要做建议预选
   （2026-08-02：用户说标签先不做，预选功能继续搁置）

## 4. 环境地雷（新会话必读，全是踩过的）

- **仓库在 iCloud 托管的 `~/Desktop` 里。** 今天修了两个由此而来的故障：清掉 457M Xcode 产物（`git status` 从卡死变 0.036s）；删掉 `.git/COMMIT_EDITMSG` 死空壳（它让 `git commit` 永久挂死）。**再遇 git 卡死，先找 `.git/` 里 `blocks=0` 的文件**：
  `find .git -type f -exec stat -f "%b %z %N" {} \; | awk '$1==0 && $2>0'`
- **`gh pr merge --delete-branch` 危险**：它触发的本地 checkout 曾以 `mmap 失败` 死在一半，把工作区文件删掉、`schema.prisma` 截成 0 字节。合并时**不要带 `--delete-branch`**，本地同步自己来。修复：`git restore --source=HEAD -- .`
- **push/fetch 要绕开死代理**：`git -c http.proxy= -c https.proxy= push`（`~/.gitconfig` 指着 127.0.0.1:33210，通常没在监听）
- **Claude 侧到服务器：HTTP 通（~240ms），SSH 不通。** 所以线上验证 Claude 自己能做，任何要 shell 的事必须用户跑。`~/.ssh/` 里只有 codetrade 那台机器的密钥
- **本机 curl 一律 `--noproxy '*'`**
- **本机没有任何 Claude API 凭证**：无 `ANTHROPIC_API_KEY`、无 `ant` profile、`claude` CLI 的 OAuth token 已过期。这正是 P4 改成「提示词交给用户的 AI」的现实原因之一
- **服务器目录仍是 `/opt/see-me`，刻意不改名**：Compose 从目录名派生项目名并用它做卷名前缀，改目录会让它找不到 `see-me_db-data` 而在**空库**上静默启动。compose 里已把 `name: see-me` 钉死，所以真要改也不会丢数据
- 本地开发：`cd backend && npm run dev`（embedded PG 在 `~/.seeme-pgdata`，Hono :3000，OTP 打终端）。sandbox 会挡 Prisma 引擎下载和 PG 启动，用 `dangerouslyDisableSandbox`
- 仓库**已公开**：`.env`、真实密钥、服务器 IP+密钥组合、用户手机号一律不入库

## 5. 用户协作风格

不要 TDD / 不要仪式（**权限引擎除外**，那是唯一强制测试区）；feature-driven；完成即 commit；
收尾走 PR + `gh pr merge` 由 Claude 直接合并；vanilla JS / Swift 是强项；会点破 scope creep；
决策提问每会话 ≤3-4 轮，其余自己拍。
